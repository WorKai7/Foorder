info:
  name: createProduct
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/products/create
  body:
    type: json
    data: |-
      {
        "name": "Sandwich jambon beurre",
        "price": 3.50
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
