info:
  name: getProduct
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/products/:productId
  params:
    - name: productId
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
