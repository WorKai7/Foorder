info:
  name: makeGiven
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:3000/orders/given/:orderId
  params:
    - name: orderId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
