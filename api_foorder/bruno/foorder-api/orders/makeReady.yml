info:
  name: makeReady
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/orders/ready/:orderId
  params:
    - name: orderId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
