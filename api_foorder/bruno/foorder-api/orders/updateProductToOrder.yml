info:
  name: updateProductToOrder
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/orders/products/update/:orderId
  params:
    - name: orderId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "products": [
          {
            "product_id": 1,
            "quantite": 1
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
