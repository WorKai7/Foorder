info:
  name: createIngredient
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/ingredients/
  body:
    type: json
    data: |-
      {
        "name": "Jambon",
        "stock": 35
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
