info:
  name: deleteIngredient
  type: http
  seq: 1

http:
  method: DELETE
  url: http://localhost:3000/ingredients/:ingredientId
  params:
    - name: ingredientId
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
